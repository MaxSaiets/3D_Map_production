// Minimal stub for unit tests — real decoding isn't exercised (GLTFLoader stub
// never parses binary buffers), only that setMeshoptDecoder(MeshoptDecoder) is callable.
module.exports = { MeshoptDecoder: {} }
