/**
 * Кольори філаментів, якими виріб РЕАЛЬНО друкується.
 *
 * Єдине джерело правди для всіх превʼю (2D-конструктор, «як у житті», слайсер,
 * жива OSM-мапа на брелку). Мусить збігатися з backend `LAYER_COLORS`
 * у `services/flat_plate_pipeline.py` — інакше превʼю обіцяє одне, а з принтера
 * виходить інше.
 *
 * Розкладка AMS у власника:
 *   білий  — основа, земля, будинки
 *   чорний — дороги, залізниця, ободок, напис
 *   зелений — зелені зони
 *   синій  — вода
 */
export const PRINT_COLORS = {
  /** [242,242,242] — біла основа/земля */
  base: "#f2f2f2",
  /**
   * Будинки друкуються ТИМ САМИМ білим філаментом, що й основа — на готовому
   * виробі їх видно за висотою (0.8-2.2мм) і тінню. Але превʼю пласке, тому
   * білим по білому вони зливались. Беремо ледь сіріший відтінок: читається як
   * білий, але обʼєм видно (фідбек власника: «трохи сірішими, але щоб білі»).
   */
  buildings: "#e4e4e4",
  /** Контур будинку — головне, що робить його помітним на пласкому превʼю */
  buildingEdge: "#9a9a9a",
  /** [20,20,20] — чорні дороги (і залізниця) */
  roads: "#141414",
  /** [25,25,25] — чорний ободок по краю */
  rim: "#191919",
  /** [25,25,25] — чорний напис */
  text: "#191919",
  /** [47,111,214] — синя вода */
  water: "#2f6fd6",
  /** [63,122,63] — зелені зони */
  parks: "#3f7a3f",
  /** [206,38,38] — підсвічений будинок (окрема вставка) */
  highlight: "#ce2626",
  /** [201,144,47] — визначні місця (бронза) */
  landmark: "#c9902f",
} as const;

/** Тло «повітря» у превʼю — крізь отвір петлі та поза корпусом. */
export const PREVIEW_VOID = "#050a18";
